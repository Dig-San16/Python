#Reduce é usado para aplicar uma função cumulativa a todos os itens de uma
#sequência (como uma lista), de modo a reduzir a sequência a um único valor,
#muito parecido com o esquema do SUM.

from functools import reduce

variável = reduce(função reduzida, coleção)

#Reduce aplica uma função a todos os elementos de uma coleção, retornando apenas
#o elemento
