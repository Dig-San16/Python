#A função filter é usada para filtrar elementos de uma coleção
#(como uma lista, array ou sequência) com base em uma condição específica.

variavél = filter(função, coleção)

#Filter é responsavel por aplicar uma função lógica(que retorna um booleano)
#em todos os elementos de uma coleção(list, dict, etc) e retorna APENAS
#aqueles que resultam em verdadeiro (True)
